
New in version 2 is:

1. Suppose if first version and third versin contents are same then instead of creating new third version it 
   assumes that old version and gives msg like "already available"

2. It checks the validity of versions and also gives proper msg.

3. Validation like takes only 10 charachters including '\n' in single line and
  only max 20 lines will be present in actual text file.
  

Execution steps:

1. svc filename   #commit changes

2. svc N          #N=0....N Version number
